<%- include("../partials/head") %>

<body>
<%-include("../partials/header")%>
    <main>
        <section class="login_full">
         <div class=></div>  
            <div>
                <h2>Inicia sesión</h2>  
            <div class="center">
                <form class="formulario_login" method="post">
                    <% if (locals.errors && errors.login) { %>
                        <div class="error"><%= errors.login.msg %> </div>
                    <% } %>
                    <div class="txt_field">
                        <input id="email_login" type="text" name ="email" required>
                        <div class="dinamic-mail-feedback"></div>
                        <span></span>
                        <label>Email</label>
                    </div>
                    <div class="txt_field">
                        <input id="password_login" name ="password" type="password" required>
                        <div class="dinamic-password-feedback"></div>
                        <span></span>
                        <label>Contraseña</label>
                    </div>
                    <div class="forgot">Olvidaste la contraseña?</div>
                    <input type="checkbox" name="recordar" id="recordar" class="recordar">Recordar Usuario
                    <input type="submit" value="Iniciar sesión">
                    <div class="link_de_inicio">
                        No sos miembro? <a href="/users/register">Regristrate</a>
                    </div>
                </form>
            </div>
        </div>
        </section>
    </main>
    <script src="../js/login.js">
    </script>
</body>
</html>
